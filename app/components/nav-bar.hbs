 <!-- Navbar Start -->
        <nav class="navbar navbar-expand-lg bg-white navbar-light shadow sticky-top p-0">
            <a href="" {{on "click" (fn this.handleClick "home")}} class="navbar-brand d-flex align-items-center text-center py-0 px-4 px-lg-5">
                <h1 class="m-0 text-primary">&nbsp;&nbsp;&nbsp;&nbsp;JobiFy</h1>
            </a>
            <button type="button" class="navbar-toggler me-4" data-bs-toggle="collapse" data-bs-target="#navbarCollapse">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarCollapse">
                <div class="navbar-nav ms-auto p-4 p-lg-0">
                    <a href="" {{on "click" (fn this.handleClick "home")}} class="nav-item nav-link {{selected-nav this.currentRoute 'home'}} ">Home</a>
                    <a href="" {{on "click" (fn this.handleClick "about")}} class="nav-item nav-link {{selected-nav this.currentRoute 'about'}}">About</a>
                    <div class="nav-item dropdown">
                        <a href="#" class="nav-link dropdown-toggle {{selected-nav this.currentRoute 'job-list'}} {{selected-nav this.currentRoute 'job-detail'}}" data-bs-toggle="dropdown">Jobs</a>
                        <div class="dropdown-menu rounded-0 m-0 ">
                            <a href="" {{on "click" (fn this.handleClick "job-list")}} class="dropdown-item {{selected-nav this.currentRoute 'job-list'}}">Job List</a>
                            <a href="" {{on "click" (fn this.handleClick "job-detail")}} class="dropdown-item {{selected-nav this.currentRoute 'job-detail'}}">Job Detail</a>
                        </div>
                    </div>
                    <div class="nav-item dropdown">
                        <a href="#" class="nav-link dropdown-toggle {{selected-nav this.currentRoute 'category'}} {{selected-nav this.currentRoute 'testimonial'}} {{selected-nav this.currentRoute 'not-found'}}" data-bs-toggle="dropdown">Pages</a>
                        <div class="dropdown-menu rounded-0 m-0">
                            <a href=""{{on "click" (fn this.handleClick "category")}} class="dropdown-item {{selected-nav this.currentRoute 'category'}}">Job Category</a>
                            <a href=""{{on "click" (fn this.handleClick "testimonial")}} class="dropdown-item {{selected-nav this.currentRoute 'testimonial'}}">Testimonial</a>
                            <a href="" {{on "click" (fn this.handleClick "not-found")}} class="dropdown-item {{selected-nav this.currentRoute 'not-found'}}">404</a>
                        </div>
                    </div>
                    <a href="" {{on "click" (fn this.handleClick "contact")}} class="nav-item nav-link">Contact</a>
                </div>
                <a href="" {{on "click" (fn this.handleClick "company-login")}} class="btn btn-primary rounded-0 py-4 px-lg-5 d-none d-lg-block">Post A Job<i class="fa fa-arrow-right ms-3"></i></a>
            </div>
        </nav>
        <!-- Navbar End -->
        {{yield}}